# 2026-09-05 复核记录

基线：`4d127d3bb258c954105d46e2357d361b38c6f589`。本次检查及命令由 Codex 执行，维护者本人自审另见 PR 勾选项。

## 本次检查

核对 hfutzip.pro 的源文件清单和 qmake 入口。修正仓库名称及克隆地址，移除缺少 CMakeLists.txt 支持的构建步骤和未核验的性能百分比。

## 验证边界

未配置 Qt、未编译或执行压缩往返。静态检查发现 writeVariableLength 对 128 写入单字节 0x80，而 readVariableLength 将其识别为双字节前缀，两者不一致；U 格式在该边界需要修复和回归验证。本 PR 只修正文档，不声明压缩正确性通过。
